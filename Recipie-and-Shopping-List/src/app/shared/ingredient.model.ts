export class Ingredient{
    // public name:string;
    // public amount: number;

    constructor(public name:string, public amount:number){ // same as up there , automatically assigns the name and amount
    }
}